server:
  port: 8081

spring:
  application:
    name: ms-users-service

  docker:
    compose:
      enabled: false

  data:
    mongodb:
      uri: mongodb+srv://jesusmiranda_db:H1ksXg1TzaDg6z2Z@cluster0.coow4vh.mongodb.net/Microservice_Users_Jesus?retryWrites=true&w=majority

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:https://lab.vallegrande.edu.pe/sigrc/keycloak/realms/PRS04}
          jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:https://lab.vallegrande.edu.pe/sigrc/keycloak/realms/PRS04/protocol/openid-connect/certs}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    url: /api-docs

auth-service:
  url: ${AUTH_SERVICE_URL:https://lab.vallegrande.edu.pe/sigrc/gateway}
  internal-api-key: ${AUTH_INTERNAL_API_KEY:sigrc-internal-dev-key}

logging:
  level:
    pe.edu.vallegrande.sigrc: INFO
    org.springframework.data.mongodb: INFO

cloudinary:
  cloud_name: ${CLOUDINARY_CLOUD_NAME:dwy6gb0gn}
  api_key: ${CLOUDINARY_API_KEY:973672632453524}
  api_secret: ${CLOUDINARY_API_SECRET:TJU2HYli0JLnmYLSqEbvytBgNJ4}