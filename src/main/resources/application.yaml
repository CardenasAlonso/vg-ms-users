server:
  port: 8081

spring:
  application:
    name: ms-users-service

  docker:
    compose:
      enabled: false

  data:
    mongodb:
      uri: mongodb+srv://jesusmiranda_db:H1ksXg1TzaDg6z2Z@cluster0.coow4vh.mongodb.net/Microservice_Users_Jesus?retryWrites=true&w=majority

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:https://lab.vallegrande.edu.pe/sigrc/keycloak/realms/PRS04}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    url: /api-docs

keycloak:
  admin:
    base-url: ${KEYCLOAK_BASE_URL:https://lab.vallegrande.edu.pe/sigrc/keycloak}
    realm: ${KEYCLOAK_REALM:PRS04}
    client-id: ${KEYCLOAK_ADMIN_CLIENT_ID:sigrc-gateway}
    client-secret: ${KEYCLOAK_ADMIN_CLIENT_SECRET}

logging:
  level:
    pe.edu.vallegrande.sigrc: DEBUG
    org.springframework.data.mongodb: DEBUG